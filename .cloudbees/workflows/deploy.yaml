apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: Deploy
on:
  push:
    branches:
      - "**"
jobs:
  deploy:
    steps:
      - uses: gmaxey/app-model/Deploy@main
        name: Deploy application
        kind: deploy
        with:
          environment: QA